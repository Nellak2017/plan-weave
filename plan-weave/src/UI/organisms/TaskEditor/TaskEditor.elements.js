import { styled } from '@mui/system'
export const StyledTaskEditor = styled('div')(({ theme, maxwidth }) => ({ width: '100%', maxWidth: `${maxwidth}px`, borderRadius: '36px', backgroundColor: 'black', h1: { color: theme.palette.text.primary }, }))
export const TaskEditorContainer = styled('section')(({ theme }) => ({ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', h1: { color: theme.palette.text.primary, fontWeight: 300, fontSize: theme.typography.h2.fontSize, }, }))